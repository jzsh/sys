1.
cp ./git-completion.bash ~/.git-completion.bash 

2. edit .bash_profile

if [ -f ~/.git-completion.bash ]; then 
	. ~/.git-completion.bash 
fi 

## github
cat ~/.ssh/id_rsa.pub and paste key to github website setting
change address in .git/config to "ssh://git@github.com/..."

